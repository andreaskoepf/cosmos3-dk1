diff --git a/cosmos_framework/configs/base/config.py b/cosmos_framework/configs/base/config.py
index f7fd14e..b6fa0a2 100644
--- a/cosmos_framework/configs/base/config.py
+++ b/cosmos_framework/configs/base/config.py
@@ -98,4 +98,12 @@ def make_config() -> Config:
     # Register shipped experiments explicitly.
     import cosmos_framework.configs.base.experiment.sft.vision_sft_nano  # noqa: F401
     import cosmos_framework.configs.base.experiment.sft.vision_sft_super  # noqa: F401
+
+    # Register external experiment modules listed in $COSMOS_EXTRA_EXPERIMENTS
+    # (comma-separated importable module names; must be on PYTHONPATH). Lets
+    # out-of-tree projects (e.g. cosmos-dk1) register their experiments without
+    # editing this file. Their cs.store(group="experiment", ...) side-effects fire on import.
+    import importlib, os  # noqa: E401
+    for _mod in filter(None, (m.strip() for m in os.environ.get("COSMOS_EXTRA_EXPERIMENTS", "").split(","))):
+        importlib.import_module(_mod)
     return c
diff --git a/cosmos_framework/data/vfm/action/domain_utils.py b/cosmos_framework/data/vfm/action/domain_utils.py
index 917fe4c..3c71f0f 100644
--- a/cosmos_framework/data/vfm/action/domain_utils.py
+++ b/cosmos_framework/data/vfm/action/domain_utils.py
@@ -18,6 +18,8 @@ EMBODIMENT_TO_DOMAIN_ID: dict[str, int] = {
     "robomind-ur": 13,
     "agibotworld": 15,
     "fractal": 20,
+    "dk1": 25,  # DK-1 bimanual, 14D joint-space (added for cosmos-dk1 SFT)
+    "dk1_cartesian": 26,  # DK-1 bimanual, 20D cartesian EE pose deltas (cosmos-dk1 SFT)
 }
 
 
@@ -33,6 +35,8 @@ EMBODIMENT_TO_RAW_ACTION_DIM: dict[str, int] = {
     "robomind-ur": 10,
     "agibotworld": 29,
     "fractal": 10,
+    "dk1": 14,  # left arm(6) + left gripper(1) + right arm(6) + right gripper(1)
+    "dk1_cartesian": 20,  # per arm: pos(3) + rot6d(6) + gripper(1), left then right
 }
 
 
diff --git a/cosmos_framework/model/vfm/omni_mot_model.py b/cosmos_framework/model/vfm/omni_mot_model.py
index 9e93f98..b39b9c5 100644
--- a/cosmos_framework/model/vfm/omni_mot_model.py
+++ b/cosmos_framework/model/vfm/omni_mot_model.py
@@ -983,7 +983,7 @@ class OmniMoTModel(ImaginaireModel):
                     "Action condition mask must be a list of tensors for loss computation"
                 )
                 assert gen_data_noised.vt_target_action is not None, "Action targets required when action_gen is True"
-                fm_loss_action, _ = self._compute_flow_matching_loss(
+                fm_loss_action, fm_loss_action_per_instance = self._compute_flow_matching_loss(
                     pred=out_net["preds_action"],
                     target=gen_data_noised.vt_target_action,
                     condition_mask=data_batch_packed.action.condition_mask,
@@ -997,6 +997,9 @@ class OmniMoTModel(ImaginaireModel):
                 # Yihuai: In case the video loss is too large (1.5) and covers the action loss (0.05), we scale up the action loss to match the video loss to improve action precision.
                 total_loss += fm_loss_action * rf_cfg.action_loss_weight
                 losses_dict["flow_matching_loss_action"] = fm_loss_action
+                # Per-sample action loss (was discarded) — enables per-mode loss bucketing
+                # in PerModeLossCallback (aligned with data_batch["mode"]).
+                losses_dict["flow_matching_loss_action_per_instance"] = fm_loss_action_per_instance
             else:
                 # No action data in this batch. Connect the network's dummy preds_action
                 # to the loss so action-specific params
@@ -4266,12 +4269,27 @@ class OmniMoTModel(ImaginaireModel):
         from cosmos_framework.utils.vfm.lora import inject_lora_pre_fsdp
 
         self.lora_alpha = lora_alpha
-        return inject_lora_pre_fsdp(
+        network = inject_lora_pre_fsdp(
             network,
             lora_rank=lora_rank,
             lora_alpha=lora_alpha,
             lora_target_modules=lora_target_modules,
         )
+        # Un-freeze selected non-LoRA modules so they train fully alongside LoRA.
+        # LoRA injection froze every non-adapter param; a brand-new embodiment's
+        # action projection (action2llm/llm2action) must be LEARNED, not adapted.
+        # Driven by $LORA_ALSO_TRAIN (comma-separated name substrings).
+        import os
+        also = [t.strip() for t in os.environ.get("LORA_ALSO_TRAIN", "").split(",") if t.strip()]
+        if also:
+            n = 0
+            for name, p in network.named_parameters():
+                if any(t in name for t in also):
+                    p.requires_grad_(True)
+                    n += p.numel()
+            from cosmos_framework.utils import log
+            log.info(f"LoRA: also training {n:,} non-LoRA params matching {also}")
+        return network
 
     def _init_lora_weights_post_materialization(self, network: torch.nn.Module) -> None:
         """Post-materialization LoRA init — see :func:`init_lora_weights_post_materialization`."""
diff --git a/cosmos_framework/utils/vfm/lora.py b/cosmos_framework/utils/vfm/lora.py
index 8bffe56..5900944 100644
--- a/cosmos_framework/utils/vfm/lora.py
+++ b/cosmos_framework/utils/vfm/lora.py
@@ -74,15 +74,26 @@ def _inject_lora_inplace(
 ) -> int:
     """Replace each ``<target>`` ``nn.Linear`` child in-place with ``LoraInjectedLinear``.
 
-    Match is by exact child name (e.g., ``q_proj_moe_gen``), not substring.
+    Two target forms are supported:
+      - bare leaf name (e.g. ``q_proj_moe_gen``) — matches by exact child name.
+      - dotted path suffix (e.g. ``mlp_moe_gen.gate_proj``) — matches when the
+        child's FULL module path ends with the target. This disambiguates leaves
+        that are shared across experts (the gen MLP ``mlp_moe_gen.gate_proj`` vs the
+        frozen reasoner ``mlp.gate_proj`` both have leaf ``gate_proj``), so MLP LoRA
+        can be confined to the generation expert.
+
     Snapshots ``named_modules()`` before mutating the tree so newly-inserted
     LoRA submodules are not re-visited.
     """
-    target_set = set(target_modules)
+    leaf_targets = {t for t in target_modules if "." not in t}
+    path_targets = [t for t in target_modules if "." in t]
     replaced = 0
-    for _parent_name, parent in list(network.named_modules()):
+    for parent_name, parent in list(network.named_modules()):
         for child_name, child in list(parent.named_children()):
-            if child_name in target_set and isinstance(child, nn.Linear):
+            if not isinstance(child, nn.Linear):
+                continue
+            full_name = f"{parent_name}.{child_name}" if parent_name else child_name
+            if child_name in leaf_targets or any(full_name.endswith(pt) for pt in path_targets):
                 setattr(parent, child_name, LoraInjectedLinear(child, rank, alpha))
                 replaced += 1
     return replaced
@@ -121,8 +132,14 @@ def inject_lora_pre_fsdp(
     if not target_modules_list:
         raise ValueError("LoRA target_modules cannot be empty")
 
-    model_module_names = {name.split(".")[-1] for name, _ in network.named_modules()}
-    invalid_modules = [t for t in target_modules_list if t not in model_module_names]
+    all_module_names = [name for name, _ in network.named_modules()]
+    leaf_module_names = {name.split(".")[-1] for name in all_module_names}
+    invalid_modules = [
+        t
+        for t in target_modules_list
+        if (t not in leaf_module_names)
+        and not (("." in t) and any(n.endswith(t) for n in all_module_names))
+    ]
     if invalid_modules:
         log.warning(f"LoRA target modules not found in model: {invalid_modules}")
 
